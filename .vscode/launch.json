{
  "version": "0.2.0",
  "configurations": [
        {
      "name": "Build and Run",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceRoot}/build/src/myapp",
      "args": ["3"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build"
      //,"internalConsoleOptions": "openOnSessionStart"
    }
  ]
}
